---
title: 'MDX Components Test Page'
description: 'Test page for all MDX components'
lead: 'Testing all available MDX components'
category: 'root'
hidden: true
---

export const CurrentProduct = ({product}) => (
    <div className="bg-blue-50 border border-blue-200 rounded-lg p-4 my-4">
        <p className="text-blue-800">
            You are viewing this page in the <strong>{product || 'default'}</strong> product context.
        </p>
    </div>
);

## MDX Components Test Page

<CurrentProduct product={props.product}/>

This page tests all available MDX components that are globally available through our configuration.

## Callout Component

<Callout type="info" title="Information Callout">
    This is an info callout with a description. It provides helpful information to users.
</Callout>

<Callout type="warning" title="Warning Callout">
    This is a warning callout. It alerts users to potential issues or important considerations.
</Callout>

<Callout type="error" title="Error Callout">
    This is an error callout. It highlights critical issues or errors.
</Callout>

<Callout type="info" title="Another Info Example">
    You can have multiple callouts of the same type with different content.
</Callout>

<Callout type="warning" title="Empty Description Test">

</Callout>

## PerProduct Component

<PerProduct includes={["rails"]}>
    Rails Only Content
</PerProduct>

<PerProduct includes={["sites"]}>
    Sites Only Content
</PerProduct>

<PerProduct includes={["rails", "sites"]}>
    Rails and Sites Content
</PerProduct>

<PerProduct excludes={["rails"]}>
    Content for all products except Rails
</PerProduct>

## Tabs Component

<Tabs defaultValue="first-tab">
    <TabsList>
        <TabsTrigger value="first-tab">First Tab</TabsTrigger>
        <TabsTrigger value="second-tab">Second Tab</TabsTrigger>
        <TabsTrigger value="third-tab">Third Tab</TabsTrigger>
    </TabsList>
    <TabsContent value="first-tab">
        This is the content of the first tab. You can put any markdown or components here.

        - List item 1
        - List item 2
        - List item 3
    </TabsContent>
    <TabsContent value="second-tab">
        This is the content of the second tab.

        No code
    </TabsContent>
    <TabsContent value="third-tab">
        This is the content of the third tab.

        <Callout type="info" title="Nested Components">
            You can even nest other components inside tabs!
        </Callout>
    </TabsContent>
</Tabs>

## Table Component


| Feature | Description | Status |
|---------|------------|--------|
| Auto-scaling | Automatically scale your infrastructure | ✅ Active |
| Monitoring | Built-in monitoring and alerts | ✅ Active |
| Backups | Automated backup system | ✅ Active |
| SSL Certificates | Free SSL certificates | ✅ Active |


## Collapsible Component

<Collapsible title="Click to expand this section">
    This content is hidden by default and can be expanded by clicking on the title.

    You can put any content here including:
    - Lists
    - **Bold text**
    - `Code snippets`

    Even other components:

    <Callout type="info" title="Nested in Collapsible">
        Components can be nested inside collapsibles!
    </Callout>
</Collapsible>

### An indented collapsible section

<Collapsible title="Another collapsible section">
    This is another collapsible section with different content.

    ```bash
    # You can include code blocks
    echo "Hello from inside a collapsible!"
    ```
</Collapsible>

## Hint Component

Here's some text with a <Hint caption="This is additional information that appears on hover">hint embedded inline</Hint> that provides extra context when you hover over it.

## Glyph Component

Here are some glyphs with different colors:

- Check glyph: <Glyph icon="check" color="green" />
- Warning glyph: <Glyph icon="warning" color="amber" />
- Book glyph: <Glyph icon="book" color="green" />
- Help glyph: <Glyph icon="help" color="green" />
- Build glyph: <Glyph icon="build" color="blue" />
- Upgrade glyph: <Glyph icon="upgrade" color="blue" />
- Redeploy glyph: <Glyph icon="redeploy" color="red" />

## Pill Component

Status indicators using pills:

<Pill title="Active" color="green"/> <Pill title="Pending" color="yellow"/> <Pill title="Inactive" color="gray"/> <Pill
    title="Error" color="red"/> <Pill title="Beta" color="blue"/>

## ComponentVersion Component

With specific names: <ComponentVersion names={["redis"]}/>

Tabular format:

<ComponentVersion tabular={true} names={["redis", "mongodb"]}/>

## IpList Component

<IpList/>

## Figure Component

<Figure
    src="/images/about-rails-app.png"
    alt="Example figure"
    caption="This is a figure with a caption explaining the image"
/>

<Figure
    src="/images/about_static_app.png"
    alt="Figure without caption"
/>

## FilteredContentBox and FilteredContent

<FilteredContentBox
    labels={["production", "staging", "development"]}
    placeholder="Select environment"
>
    <FilteredContent filter="production">
        This content is filtered for production environments.
    </FilteredContent>

    <FilteredContent filter="staging">
        This content is filtered for staging environments.
    </FilteredContent>

    <FilteredContent filter="development">
        This content is filtered for development environments.
    </FilteredContent>
</FilteredContentBox>

## Interactive React Components

Here are some interactive button components:

<div className="my-6 flex gap-4">
    <Button variant="default">Default Button</Button>
    <Button variant="outline">Outline Button</Button>
    <Button variant="secondary">Secondary Button</Button>
    <Button variant="destructive">Destructive Button</Button>
</div>

## Inline Code

Here is some inline code: `const example = "Hello, Cloud66!";` within a sentence.

## Code Blocks with Shiki Syntax Highlighting

### Bash
```bash
#!/bin/bash
# Deploy with Cloud 66 CLI

# Deploy the stack
cx stacks deploy --stack my-app

# Check deployment status
cx stacks show my-app --status

# View application logs
cx logs --stack my-app --tail
```

### YAML
```yaml
# Docker Compose configuration
version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./public:/usr/share/nginx/html
  
  app:
    build: .
    environment:
      - RAILS_ENV=production
      - DATABASE_URL=postgresql://user:pass@db:5432/myapp
    depends_on:
      - db
  
  db:
    image: postgres:14
    environment:
      - POSTGRES_PASSWORD=secretpass
```

### JSON
```json
{
  "name": "my-cloud66-app",
  "version": "1.0.0",
  "description": "Cloud66 deployment configuration",
  "scripts": {
    "start": "node server.js",
    "deploy": "cx stacks deploy --stack production"
  },
  "dependencies": {
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  }
}
```

### Dockerfile
```dockerfile
# Multi-stage Dockerfile example
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY . .
EXPOSE 3000
USER node
CMD ["node", "server.js"]
```

## Direct CodeBlock Component Usage

You can also use the CodeBlock component directly for more control over the display:

### Basic CodeBlock with title

<CodeBlock language="javascript" title="example.js">
{`// Direct CodeBlock component usage
const greeting = "Hello, World!";

function sayHello(name) {
    return \`Hello, \${name}!\`;
}

console.log(sayHello("Cloud66"));`}
</CodeBlock>

### CodeBlock with line numbers

<CodeBlock
    language="python"
    title="app.py"
    showLineNumbers={true}
>
{`# Python example with line numbers
import os
from flask import Flask

app = Flask(__name__)

@app.route('/')
def hello():
    return "Hello from Cloud66!"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)`}
</CodeBlock>

### CodeBlock with line numbers (longer example)

<CodeBlock
    language="javascript"
    title="server.js"
    showLineNumbers={true}
>
{`// Express server example with line numbers
const express = require('express');
const path = require('path');
const helmet = require('helmet');
const cors = require('cors');

const app = express();
const PORT = process.env.PORT || 3000;

// Middleware
app.use(helmet());
app.use(cors());
app.use(express.json());
app.use(express.urlencoded({ extended: true }));
app.use(express.static(path.join(__dirname, 'public')));

// Routes
app.get('/', (req, res) => {
    res.json({ message: 'Welcome to Cloud66 API' });
});

app.get('/health', (req, res) => {
    res.status(200).json({ status: 'healthy' });
});

// Error handling
app.use((err, req, res, next) => {
    console.error(err.stack);
    res.status(500).send('Something broke!');
});

// Start server
app.listen(PORT, () => {
    console.log(\`Server is running on port \${PORT}\`);
});`}
</CodeBlock>

### CodeBlock with Shiki transformers

<CodeBlock
    language="typescript"
    title="advanced.ts">
    {`// TypeScript example with highlighted lines
interface User {
  id: number       // Line 3 - highlighted
  name: string     // Line 4 - highlighted
  email: string    // Line 5 - highlighted
  active: boolean
}

function getUser(id: number): User {
  return {
        id,
        name: "John Doe",
        email: "john@example.com",
        active: true
    }
}`}
</CodeBlock>

### CodeBlock with custom theme

<CodeBlock
    language="javascript"
    title="custom-theme.js">
    {`// Using a different theme (min-light)
const config = loadConfig();

function processData(data) {
    return data.map(item => item * 2);
}

export { processData };`}
</CodeBlock>

### CodeBlock with multiple Shiki features

<CodeBlock
    language="typescript"
    title="full-example.ts">
    {`// First line will be bold
function greet(name) {
    console.log(\`Hello, \${name}!\`);
}

greet("Cloud66");`}
</CodeBlock>

### CodeBlock without copy button

<CodeBlock
    language="bash"
    showCopyButton={false}
    title="Terminal Output"
>
    {`$ cx stacks list
NAME            STATUS    CREATED
my-app          Running   2024-01-15
staging-app     Stopped   2024-01-10
production      Running   2024-01-01`}
</CodeBlock>

---